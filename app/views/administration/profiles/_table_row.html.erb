<%= form_for profile, url: send_email_administration_profile_url(profile.id), method: 'get', html: {class: ""} do |f| %>
	<tr>
		<td> <%= profile.user.email %></td>
		<td>
			<%= f.submit "Send", class: "btn btn-secondary"  %>
		</td>
	</tr>
<% end %>
